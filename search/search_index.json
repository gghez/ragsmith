{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ragsmith","text":"<p>Async RAG toolkit on top of Postgres + pgvector and Voyage AI.</p> <p>See the API Reference for the full module documentation, or browse the README for a quick start.</p>"},{"location":"api/SUMMARY/","title":"SUMMARY","text":"<ul> <li>ragsmith<ul> <li>chunking</li> <li>embeddings</li> <li>retriever</li> <li>store</li> </ul> </li> </ul>"},{"location":"api/ragsmith/","title":"<code>ragsmith</code>","text":""},{"location":"api/ragsmith/#ragsmith","title":"ragsmith","text":"<p>ragsmith \u2014 async RAG toolkit on Postgres/pgvector and Voyage AI.</p>"},{"location":"api/ragsmith/#ragsmith.Chunk","title":"Chunk  <code>dataclass</code>","text":"<pre><code>Chunk(index: int, text: str, start: int, end: int)\n</code></pre> <p>A contiguous slice of text produced by <code>chunk_text</code>.</p> <p>Attributes:</p> Name Type Description <code>index</code> <code>int</code> <p>Zero-based position of the chunk in the source document.</p> <code>text</code> <code>str</code> <p>The chunk text content.</p> <code>start</code> <code>int</code> <p>Character offset of the chunk start inside the source text.</p> <code>end</code> <code>int</code> <p>Character offset of the chunk end (exclusive) inside the source text.</p>"},{"location":"api/ragsmith/#ragsmith.Document","title":"Document  <code>dataclass</code>","text":"<pre><code>Document(\n    content: str,\n    embedding: list[float],\n    metadata: dict[str, object] = dict(),\n    doc_id: UUID = uuid4(),\n)\n</code></pre> <p>A chunk persisted in the vector store.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>str</code> <p>Raw text content.</p> <code>embedding</code> <code>list[float]</code> <p>Embedding vector (length must match the store <code>dim</code>).</p> <code>metadata</code> <code>dict[str, object]</code> <p>Arbitrary JSON-serializable metadata.</p> <code>doc_id</code> <code>UUID</code> <p>Stable identifier (auto-generated when omitted).</p>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore","title":"PgVectorStore","text":"<pre><code>PgVectorStore(pool: Pool, *, table: str, dim: int)\n</code></pre> <p>Async pgvector-backed document store.</p> <p>The store creates (idempotently) a single table with an <code>ivfflat</code> index on cosine distance. Use <code>from_dsn</code> to obtain a fully initialized instance.</p> <p>Build a new store bound to an existing pool.</p> <p>Parameters:</p> Name Type Description Default <code>pool</code> <code>Pool</code> <p>An <code>asyncpg</code> connection pool.</p> required <code>table</code> <code>str</code> <p>Destination table name (validated to be a SQL identifier).</p> required <code>dim</code> <code>int</code> <p>Embedding dimensionality.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>table</code> is not a safe identifier or <code>dim</code> &lt;= 0.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>def __init__(self, pool: asyncpg.Pool, *, table: str, dim: int) -&gt; None:\n    \"\"\"Build a new store bound to an existing pool.\n\n    Args:\n        pool: An ``asyncpg`` connection pool.\n        table: Destination table name (validated to be a SQL identifier).\n        dim: Embedding dimensionality.\n\n    Raises:\n        ValueError: If ``table`` is not a safe identifier or ``dim`` &lt;= 0.\n    \"\"\"\n    if not table.replace(\"_\", \"\").isalnum():\n        msg = f\"unsafe table name: {table!r}\"\n        raise ValueError(msg)\n    if dim &lt;= 0:\n        msg = \"dim must be &gt; 0\"\n        raise ValueError(msg)\n    self._pool = pool\n    self._table = table\n    self._dim = dim\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Enter async context manager.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter async context manager.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the underlying pool.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None:\n    \"\"\"Close the underlying pool.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying connection pool.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying connection pool.\"\"\"\n    await self._pool.close()\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.from_dsn","title":"from_dsn  <code>async</code> <code>classmethod</code>","text":"<pre><code>from_dsn(\n    dsn: str,\n    *,\n    table: str = \"ragsmith_chunks\",\n    dim: int = 1024,\n) -&gt; Self\n</code></pre> <p>Create a store and the underlying pool from a DSN.</p> <p>Parameters:</p> Name Type Description Default <code>dsn</code> <code>str</code> <p>Postgres connection string.</p> required <code>table</code> <code>str</code> <p>Destination table name.</p> <code>'ragsmith_chunks'</code> <code>dim</code> <code>int</code> <p>Embedding dimensionality.</p> <code>1024</code> <p>Returns:</p> Type Description <code>Self</code> <p>An initialized <code>PgVectorStore</code>.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>@classmethod\nasync def from_dsn(\n    cls,\n    dsn: str,\n    *,\n    table: str = \"ragsmith_chunks\",\n    dim: int = 1024,\n) -&gt; Self:\n    \"\"\"Create a store and the underlying pool from a DSN.\n\n    Args:\n        dsn: Postgres connection string.\n        table: Destination table name.\n        dim: Embedding dimensionality.\n\n    Returns:\n        An initialized [`PgVectorStore`][ragsmith.store.PgVectorStore].\n    \"\"\"\n    import asyncpg  # noqa: PLC0415\n\n    pool = await asyncpg.create_pool(dsn=dsn)\n    store = cls(pool, table=table, dim=dim)\n    await store.initialize()\n    return store\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.initialize","title":"initialize  <code>async</code>","text":"<pre><code>initialize() -&gt; None\n</code></pre> <p>Create the pgvector extension, table and index if missing.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def initialize(self) -&gt; None:\n    \"\"\"Create the pgvector extension, table and index if missing.\"\"\"\n    async with self._pool.acquire() as conn:\n        await conn.execute(\"CREATE EXTENSION IF NOT EXISTS vector\")\n        await conn.execute(\n            f\"CREATE TABLE IF NOT EXISTS {self._table} (\"\n            \"  id UUID PRIMARY KEY,\"\n            \"  content TEXT NOT NULL,\"\n            \"  metadata JSONB NOT NULL DEFAULT '{}'::jsonb,\"\n            f\"  embedding vector({self._dim}) NOT NULL\"\n            \")\",\n        )\n        await conn.execute(\n            f\"CREATE INDEX IF NOT EXISTS {self._table}_embedding_idx \"\n            f\"ON {self._table} USING ivfflat (embedding vector_cosine_ops)\",\n        )\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.similarity_search","title":"similarity_search  <code>async</code>","text":"<pre><code>similarity_search(\n    embedding: Sequence[float], *, k: int = 5\n) -&gt; list[tuple[Document, float]]\n</code></pre> <p>Return the <code>k</code> documents closest to <code>embedding</code> (cosine distance).</p> <p>Parameters:</p> Name Type Description Default <code>embedding</code> <code>Sequence[float]</code> <p>Query vector.</p> required <code>k</code> <code>int</code> <p>Number of results.</p> <code>5</code> <p>Returns:</p> Type Description <code>list[tuple[Document, float]]</code> <p>Pairs of (document, distance) sorted by ascending distance.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def similarity_search(\n    self,\n    embedding: Sequence[float],\n    *,\n    k: int = 5,\n) -&gt; list[tuple[Document, float]]:\n    \"\"\"Return the ``k`` documents closest to ``embedding`` (cosine distance).\n\n    Args:\n        embedding: Query vector.\n        k: Number of results.\n\n    Returns:\n        Pairs of (document, distance) sorted by ascending distance.\n    \"\"\"\n    async with self._pool.acquire() as conn:\n        records = await conn.fetch(\n            f\"SELECT id, content, metadata, embedding, \"  # noqa: S608\n            f\"embedding &lt;=&gt; $1::vector AS distance \"\n            f\"FROM {self._table} ORDER BY distance ASC LIMIT $2\",\n            _vector_literal(embedding),\n            k,\n        )\n    results: list[tuple[Document, float]] = []\n    for record in records:\n        doc = Document(\n            doc_id=UUID(str(record[\"id\"])),\n            content=record[\"content\"],\n            metadata=json.loads(record[\"metadata\"]) if record[\"metadata\"] else {},\n            embedding=[],\n        )\n        results.append((doc, float(record[\"distance\"])))\n    return results\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.PgVectorStore.upsert","title":"upsert  <code>async</code>","text":"<pre><code>upsert(documents: Iterable[Document]) -&gt; int\n</code></pre> <p>Insert or update documents.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[Document]</code> <p>Documents to persist.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The number of rows written.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def upsert(self, documents: Iterable[Document]) -&gt; int:\n    \"\"\"Insert or update documents.\n\n    Args:\n        documents: Documents to persist.\n\n    Returns:\n        The number of rows written.\n    \"\"\"\n    rows = [\n        (str(d.doc_id), d.content, json.dumps(d.metadata), _vector_literal(d.embedding))\n        for d in documents\n    ]\n    if not rows:\n        return 0\n    async with self._pool.acquire() as conn:\n        await conn.executemany(\n            f\"INSERT INTO {self._table} (id, content, metadata, embedding) \"  # noqa: S608\n            \"VALUES ($1, $2, $3::jsonb, $4::vector) \"\n            \"ON CONFLICT (id) DO UPDATE SET \"\n            \"content = EXCLUDED.content, \"\n            \"metadata = EXCLUDED.metadata, \"\n            \"embedding = EXCLUDED.embedding\",\n            rows,\n        )\n    return len(rows)\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.RetrievedChunk","title":"RetrievedChunk  <code>dataclass</code>","text":"<pre><code>RetrievedChunk(document: Document, score: float)\n</code></pre> <p>A single retrieval result.</p> <p>Attributes:</p> Name Type Description <code>document</code> <code>Document</code> <p>The matched document.</p> <code>score</code> <code>float</code> <p>Cosine similarity score in <code>[0, 1]</code> (<code>1 - distance</code>).</p>"},{"location":"api/ragsmith/#ragsmith.Retriever","title":"Retriever","text":"<pre><code>Retriever(embeddings: VoyageClient, store: PgVectorStore)\n</code></pre> <p>Embed a query and look it up in a <code>PgVectorStore</code>.</p> <p>Build a retriever.</p> <p>Parameters:</p> Name Type Description Default <code>embeddings</code> <code>VoyageClient</code> <p>The Voyage client used for query embeddings.</p> required <code>store</code> <code>PgVectorStore</code> <p>The destination vector store.</p> required Source code in <code>src/ragsmith/retriever.py</code> <pre><code>def __init__(self, embeddings: VoyageClient, store: PgVectorStore) -&gt; None:\n    \"\"\"Build a retriever.\n\n    Args:\n        embeddings: The Voyage client used for query embeddings.\n        store: The destination vector store.\n    \"\"\"\n    self._embeddings = embeddings\n    self._store = store\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.Retriever.retrieve","title":"retrieve  <code>async</code>","text":"<pre><code>retrieve(query: str, *, k: int = 5) -&gt; list[RetrievedChunk]\n</code></pre> <p>Embed <code>query</code> and return the top-<code>k</code> similar documents.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>User query.</p> required <code>k</code> <code>int</code> <p>Maximum number of documents to return.</p> <code>5</code> <p>Returns:</p> Type Description <code>list[RetrievedChunk]</code> <p>Ordered list of <code>RetrievedChunk</code>.</p> Source code in <code>src/ragsmith/retriever.py</code> <pre><code>async def retrieve(self, query: str, *, k: int = 5) -&gt; list[RetrievedChunk]:\n    \"\"\"Embed ``query`` and return the top-``k`` similar documents.\n\n    Args:\n        query: User query.\n        k: Maximum number of documents to return.\n\n    Returns:\n        Ordered list of [`RetrievedChunk`][ragsmith.retriever.RetrievedChunk].\n    \"\"\"\n    vectors = await self._embeddings.embed([query], input_type=\"query\")\n    hits = await self._store.similarity_search(vectors[0], k=k)\n    return [RetrievedChunk(document=doc, score=1.0 - distance) for doc, distance in hits]\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.VoyageClient","title":"VoyageClient","text":"<pre><code>VoyageClient(\n    api_key: str | None = None,\n    *,\n    model: str = DEFAULT_MODEL,\n    base_url: str = DEFAULT_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    client: AsyncClient | None = None,\n)\n</code></pre> <p>Async client for the Voyage AI embeddings endpoint.</p> <p>The client can be used as an async context manager. When the API key is omitted it is read from the <code>VOYAGE_API_KEY</code> environment variable.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>Default embedding model name used by <code>embed</code>.</p> Example <p>async def main() -&gt; list[list[float]]: ...     async with VoyageClient() as client: ...         return await client.embed([\"hello world\"])</p> <p>Build a new client.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str | None</code> <p>Voyage API key. Falls back to <code>VOYAGE_API_KEY</code> env var.</p> <code>None</code> <code>model</code> <code>str</code> <p>Default embedding model name.</p> <code>DEFAULT_MODEL</code> <code>base_url</code> <code>str</code> <p>Voyage API base URL.</p> <code>DEFAULT_BASE_URL</code> <code>timeout</code> <code>float</code> <p>Request timeout in seconds (ignored when <code>client</code> is supplied).</p> <code>DEFAULT_TIMEOUT</code> <code>client</code> <code>AsyncClient | None</code> <p>Pre-configured <code>httpx.AsyncClient</code> (mainly for tests).</p> <code>None</code> <p>Raises:</p> Type Description <code>VoyageError</code> <p>If no API key is provided or found in the environment.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>def __init__(\n    self,\n    api_key: str | None = None,\n    *,\n    model: str = DEFAULT_MODEL,\n    base_url: str = DEFAULT_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    client: httpx.AsyncClient | None = None,\n) -&gt; None:\n    \"\"\"Build a new client.\n\n    Args:\n        api_key: Voyage API key. Falls back to ``VOYAGE_API_KEY`` env var.\n        model: Default embedding model name.\n        base_url: Voyage API base URL.\n        timeout: Request timeout in seconds (ignored when ``client`` is supplied).\n        client: Pre-configured ``httpx.AsyncClient`` (mainly for tests).\n\n    Raises:\n        VoyageError: If no API key is provided or found in the environment.\n    \"\"\"\n    key = api_key or os.environ.get(\"VOYAGE_API_KEY\")\n    if not key:\n        msg = \"Voyage API key missing: pass api_key=... or set VOYAGE_API_KEY\"\n        raise VoyageError(msg)\n    self._api_key = key\n    self.model = model\n    self._base_url = base_url.rstrip(\"/\")\n    self._owns_client = client is None\n    self._client = client or httpx.AsyncClient(timeout=timeout)\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.VoyageClient.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Enter the async context manager.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter the async context manager.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.VoyageClient.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the underlying HTTP client if owned.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None:\n    \"\"\"Close the underlying HTTP client if owned.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.VoyageClient.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying HTTP client if it was created internally.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying HTTP client if it was created internally.\"\"\"\n    if self._owns_client:\n        await self._client.aclose()\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.VoyageClient.embed","title":"embed  <code>async</code>","text":"<pre><code>embed(\n    texts: list[str],\n    *,\n    model: str | None = None,\n    input_type: InputType = None,\n) -&gt; list[list[float]]\n</code></pre> <p>Embed a batch of texts.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>list[str]</code> <p>Non-empty list of strings to embed.</p> required <code>model</code> <code>str | None</code> <p>Optional model override (defaults to <code>self.model</code>).</p> <code>None</code> <code>input_type</code> <code>InputType</code> <p>Either <code>\"query\"</code>, <code>\"document\"</code> or <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[list[float]]</code> <p>List of embedding vectors aligned with the input order.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>texts</code> is empty.</p> <code>VoyageError</code> <p>If the API returns a non-2xx response.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def embed(\n    self,\n    texts: list[str],\n    *,\n    model: str | None = None,\n    input_type: InputType = None,\n) -&gt; list[list[float]]:\n    \"\"\"Embed a batch of texts.\n\n    Args:\n        texts: Non-empty list of strings to embed.\n        model: Optional model override (defaults to ``self.model``).\n        input_type: Either ``\"query\"``, ``\"document\"`` or ``None``.\n\n    Returns:\n        List of embedding vectors aligned with the input order.\n\n    Raises:\n        ValueError: If ``texts`` is empty.\n        VoyageError: If the API returns a non-2xx response.\n    \"\"\"\n    if not texts:\n        msg = \"texts must be a non-empty list\"\n        raise ValueError(msg)\n\n    payload: dict[str, object] = {\"input\": texts, \"model\": model or self.model}\n    if input_type is not None:\n        payload[\"input_type\"] = input_type\n\n    response = await self._client.post(\n        f\"{self._base_url}/embeddings\",\n        headers={\n            \"Authorization\": f\"Bearer {self._api_key}\",\n            \"Content-Type\": \"application/json\",\n        },\n        json=payload,\n    )\n    if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n        msg = f\"Voyage API error {response.status_code}: {response.text}\"\n        raise VoyageError(msg)\n\n    data = response.json().get(\"data\", [])\n    return [item[\"embedding\"] for item in data]\n</code></pre>"},{"location":"api/ragsmith/#ragsmith.chunk_text","title":"chunk_text","text":"<pre><code>chunk_text(\n    text: str, *, max_chars: int = 1000, overlap: int = 100\n) -&gt; list[Chunk]\n</code></pre> <p>Split <code>text</code> into overlapping chunks.</p> <p>The splitter prefers sentence boundaries when possible. When a single sentence is larger than <code>max_chars</code> it is hard-cut.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Source text to split.</p> required <code>max_chars</code> <code>int</code> <p>Maximum number of characters per chunk.</p> <code>1000</code> <code>overlap</code> <code>int</code> <p>Number of characters to repeat between consecutive chunks.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>Ordered list of <code>Chunk</code>. Empty list if <code>text</code> is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>max_chars</code> is not strictly positive, or if <code>overlap</code> is negative or greater than/equal to <code>max_chars</code>.</p> Example <p>chunks = chunk_text(\"Hello. World.\", max_chars=8, overlap=2) [c.text for c in chunks]['Hello.', 'World.']</p> Source code in <code>src/ragsmith/chunking.py</code> <pre><code>def chunk_text(text: str, *, max_chars: int = 1000, overlap: int = 100) -&gt; list[Chunk]:\n    \"\"\"Split ``text`` into overlapping chunks.\n\n    The splitter prefers sentence boundaries when possible. When a single\n    sentence is larger than ``max_chars`` it is hard-cut.\n\n    Args:\n        text: Source text to split.\n        max_chars: Maximum number of characters per chunk.\n        overlap: Number of characters to repeat between consecutive chunks.\n\n    Returns:\n        Ordered list of [`Chunk`][ragsmith.chunking.Chunk]. Empty list if ``text`` is empty.\n\n    Raises:\n        ValueError: If ``max_chars`` is not strictly positive, or if\n            ``overlap`` is negative or greater than/equal to ``max_chars``.\n\n    Example:\n        &gt;&gt;&gt; chunks = chunk_text(\"Hello. World.\", max_chars=8, overlap=2)\n        &gt;&gt;&gt; [c.text for c in chunks]\n        ['Hello.', 'World.']\n    \"\"\"\n    if max_chars &lt;= 0:\n        msg = \"max_chars must be &gt; 0\"\n        raise ValueError(msg)\n    if overlap &lt; 0 or overlap &gt;= max_chars:\n        msg = \"overlap must be in [0, max_chars)\"\n        raise ValueError(msg)\n\n    stripped = text.strip()\n    if not stripped:\n        return []\n\n    sentences = _SENTENCE_SPLIT.split(stripped)\n    chunks: list[Chunk] = []\n    buf = \"\"\n    buf_start = 0\n    cursor = 0\n\n    def flush(buffer: str, start: int) -&gt; None:\n        if not buffer:\n            return\n        chunks.append(\n            Chunk(index=len(chunks), text=buffer.strip(), start=start, end=start + len(buffer)),\n        )\n\n    for sentence in sentences:\n        candidate = f\"{buf} {sentence}\".strip() if buf else sentence\n        if len(candidate) &lt;= max_chars:\n            if not buf:\n                buf_start = cursor\n            buf = candidate\n            cursor = buf_start + len(buf)\n            continue\n\n        flush(buf, buf_start)\n        tail = buf[-overlap:] if overlap and buf else \"\"\n        cursor = (buf_start + len(buf)) - len(tail)\n        buf = f\"{tail} {sentence}\".strip() if tail else sentence\n        buf_start = cursor\n\n        while len(buf) &gt; max_chars:\n            cut = buf[:max_chars]\n            flush(cut, buf_start)\n            tail = cut[-overlap:] if overlap else \"\"\n            buf_start = buf_start + max_chars - len(tail)\n            buf = (tail + buf[max_chars:]).strip()\n\n        cursor = buf_start + len(buf)\n\n    flush(buf, buf_start)\n    return chunks\n</code></pre>"},{"location":"api/ragsmith/chunking/","title":"<code>ragsmith.chunking</code>","text":""},{"location":"api/ragsmith/chunking/#ragsmith.chunking","title":"ragsmith.chunking","text":"<p>Text chunking utilities.</p> <p>Provides a simple, dependency-free sliding-window chunker that splits text on paragraph and sentence boundaries while honoring a maximum character size and an overlap window.</p>"},{"location":"api/ragsmith/chunking/#ragsmith.chunking.Chunk","title":"Chunk  <code>dataclass</code>","text":"<pre><code>Chunk(index: int, text: str, start: int, end: int)\n</code></pre> <p>A contiguous slice of text produced by <code>chunk_text</code>.</p> <p>Attributes:</p> Name Type Description <code>index</code> <code>int</code> <p>Zero-based position of the chunk in the source document.</p> <code>text</code> <code>str</code> <p>The chunk text content.</p> <code>start</code> <code>int</code> <p>Character offset of the chunk start inside the source text.</p> <code>end</code> <code>int</code> <p>Character offset of the chunk end (exclusive) inside the source text.</p>"},{"location":"api/ragsmith/chunking/#ragsmith.chunking.chunk_text","title":"chunk_text","text":"<pre><code>chunk_text(\n    text: str, *, max_chars: int = 1000, overlap: int = 100\n) -&gt; list[Chunk]\n</code></pre> <p>Split <code>text</code> into overlapping chunks.</p> <p>The splitter prefers sentence boundaries when possible. When a single sentence is larger than <code>max_chars</code> it is hard-cut.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Source text to split.</p> required <code>max_chars</code> <code>int</code> <p>Maximum number of characters per chunk.</p> <code>1000</code> <code>overlap</code> <code>int</code> <p>Number of characters to repeat between consecutive chunks.</p> <code>100</code> <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>Ordered list of <code>Chunk</code>. Empty list if <code>text</code> is empty.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>max_chars</code> is not strictly positive, or if <code>overlap</code> is negative or greater than/equal to <code>max_chars</code>.</p> Example <p>chunks = chunk_text(\"Hello. World.\", max_chars=8, overlap=2) [c.text for c in chunks]['Hello.', 'World.']</p> Source code in <code>src/ragsmith/chunking.py</code> <pre><code>def chunk_text(text: str, *, max_chars: int = 1000, overlap: int = 100) -&gt; list[Chunk]:\n    \"\"\"Split ``text`` into overlapping chunks.\n\n    The splitter prefers sentence boundaries when possible. When a single\n    sentence is larger than ``max_chars`` it is hard-cut.\n\n    Args:\n        text: Source text to split.\n        max_chars: Maximum number of characters per chunk.\n        overlap: Number of characters to repeat between consecutive chunks.\n\n    Returns:\n        Ordered list of [`Chunk`][ragsmith.chunking.Chunk]. Empty list if ``text`` is empty.\n\n    Raises:\n        ValueError: If ``max_chars`` is not strictly positive, or if\n            ``overlap`` is negative or greater than/equal to ``max_chars``.\n\n    Example:\n        &gt;&gt;&gt; chunks = chunk_text(\"Hello. World.\", max_chars=8, overlap=2)\n        &gt;&gt;&gt; [c.text for c in chunks]\n        ['Hello.', 'World.']\n    \"\"\"\n    if max_chars &lt;= 0:\n        msg = \"max_chars must be &gt; 0\"\n        raise ValueError(msg)\n    if overlap &lt; 0 or overlap &gt;= max_chars:\n        msg = \"overlap must be in [0, max_chars)\"\n        raise ValueError(msg)\n\n    stripped = text.strip()\n    if not stripped:\n        return []\n\n    sentences = _SENTENCE_SPLIT.split(stripped)\n    chunks: list[Chunk] = []\n    buf = \"\"\n    buf_start = 0\n    cursor = 0\n\n    def flush(buffer: str, start: int) -&gt; None:\n        if not buffer:\n            return\n        chunks.append(\n            Chunk(index=len(chunks), text=buffer.strip(), start=start, end=start + len(buffer)),\n        )\n\n    for sentence in sentences:\n        candidate = f\"{buf} {sentence}\".strip() if buf else sentence\n        if len(candidate) &lt;= max_chars:\n            if not buf:\n                buf_start = cursor\n            buf = candidate\n            cursor = buf_start + len(buf)\n            continue\n\n        flush(buf, buf_start)\n        tail = buf[-overlap:] if overlap and buf else \"\"\n        cursor = (buf_start + len(buf)) - len(tail)\n        buf = f\"{tail} {sentence}\".strip() if tail else sentence\n        buf_start = cursor\n\n        while len(buf) &gt; max_chars:\n            cut = buf[:max_chars]\n            flush(cut, buf_start)\n            tail = cut[-overlap:] if overlap else \"\"\n            buf_start = buf_start + max_chars - len(tail)\n            buf = (tail + buf[max_chars:]).strip()\n\n        cursor = buf_start + len(buf)\n\n    flush(buf, buf_start)\n    return chunks\n</code></pre>"},{"location":"api/ragsmith/embeddings/","title":"<code>ragsmith.embeddings</code>","text":""},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings","title":"ragsmith.embeddings","text":"<p>Voyage AI embeddings client (raw HTTP, no SDK).</p> <p>Calls the Voyage REST API directly through <code>httpx</code> so we avoid pulling <code>numpy</code> (a transitive dependency of the official <code>voyageai</code> SDK).</p>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.InputType","title":"InputType  <code>module-attribute</code>","text":"<pre><code>InputType = Literal['query', 'document'] | None\n</code></pre> <p>Optional input-type hint accepted by the Voyage embeddings endpoint.</p>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageClient","title":"VoyageClient","text":"<pre><code>VoyageClient(\n    api_key: str | None = None,\n    *,\n    model: str = DEFAULT_MODEL,\n    base_url: str = DEFAULT_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    client: AsyncClient | None = None,\n)\n</code></pre> <p>Async client for the Voyage AI embeddings endpoint.</p> <p>The client can be used as an async context manager. When the API key is omitted it is read from the <code>VOYAGE_API_KEY</code> environment variable.</p> <p>Attributes:</p> Name Type Description <code>model</code> <p>Default embedding model name used by <code>embed</code>.</p> Example <p>async def main() -&gt; list[list[float]]: ...     async with VoyageClient() as client: ...         return await client.embed([\"hello world\"])</p> <p>Build a new client.</p> <p>Parameters:</p> Name Type Description Default <code>api_key</code> <code>str | None</code> <p>Voyage API key. Falls back to <code>VOYAGE_API_KEY</code> env var.</p> <code>None</code> <code>model</code> <code>str</code> <p>Default embedding model name.</p> <code>DEFAULT_MODEL</code> <code>base_url</code> <code>str</code> <p>Voyage API base URL.</p> <code>DEFAULT_BASE_URL</code> <code>timeout</code> <code>float</code> <p>Request timeout in seconds (ignored when <code>client</code> is supplied).</p> <code>DEFAULT_TIMEOUT</code> <code>client</code> <code>AsyncClient | None</code> <p>Pre-configured <code>httpx.AsyncClient</code> (mainly for tests).</p> <code>None</code> <p>Raises:</p> Type Description <code>VoyageError</code> <p>If no API key is provided or found in the environment.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>def __init__(\n    self,\n    api_key: str | None = None,\n    *,\n    model: str = DEFAULT_MODEL,\n    base_url: str = DEFAULT_BASE_URL,\n    timeout: float = DEFAULT_TIMEOUT,\n    client: httpx.AsyncClient | None = None,\n) -&gt; None:\n    \"\"\"Build a new client.\n\n    Args:\n        api_key: Voyage API key. Falls back to ``VOYAGE_API_KEY`` env var.\n        model: Default embedding model name.\n        base_url: Voyage API base URL.\n        timeout: Request timeout in seconds (ignored when ``client`` is supplied).\n        client: Pre-configured ``httpx.AsyncClient`` (mainly for tests).\n\n    Raises:\n        VoyageError: If no API key is provided or found in the environment.\n    \"\"\"\n    key = api_key or os.environ.get(\"VOYAGE_API_KEY\")\n    if not key:\n        msg = \"Voyage API key missing: pass api_key=... or set VOYAGE_API_KEY\"\n        raise VoyageError(msg)\n    self._api_key = key\n    self.model = model\n    self._base_url = base_url.rstrip(\"/\")\n    self._owns_client = client is None\n    self._client = client or httpx.AsyncClient(timeout=timeout)\n</code></pre>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageClient.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Enter the async context manager.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter the async context manager.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageClient.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the underlying HTTP client if owned.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None:\n    \"\"\"Close the underlying HTTP client if owned.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageClient.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying HTTP client if it was created internally.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying HTTP client if it was created internally.\"\"\"\n    if self._owns_client:\n        await self._client.aclose()\n</code></pre>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageClient.embed","title":"embed  <code>async</code>","text":"<pre><code>embed(\n    texts: list[str],\n    *,\n    model: str | None = None,\n    input_type: InputType = None,\n) -&gt; list[list[float]]\n</code></pre> <p>Embed a batch of texts.</p> <p>Parameters:</p> Name Type Description Default <code>texts</code> <code>list[str]</code> <p>Non-empty list of strings to embed.</p> required <code>model</code> <code>str | None</code> <p>Optional model override (defaults to <code>self.model</code>).</p> <code>None</code> <code>input_type</code> <code>InputType</code> <p>Either <code>\"query\"</code>, <code>\"document\"</code> or <code>None</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[list[float]]</code> <p>List of embedding vectors aligned with the input order.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>texts</code> is empty.</p> <code>VoyageError</code> <p>If the API returns a non-2xx response.</p> Source code in <code>src/ragsmith/embeddings.py</code> <pre><code>async def embed(\n    self,\n    texts: list[str],\n    *,\n    model: str | None = None,\n    input_type: InputType = None,\n) -&gt; list[list[float]]:\n    \"\"\"Embed a batch of texts.\n\n    Args:\n        texts: Non-empty list of strings to embed.\n        model: Optional model override (defaults to ``self.model``).\n        input_type: Either ``\"query\"``, ``\"document\"`` or ``None``.\n\n    Returns:\n        List of embedding vectors aligned with the input order.\n\n    Raises:\n        ValueError: If ``texts`` is empty.\n        VoyageError: If the API returns a non-2xx response.\n    \"\"\"\n    if not texts:\n        msg = \"texts must be a non-empty list\"\n        raise ValueError(msg)\n\n    payload: dict[str, object] = {\"input\": texts, \"model\": model or self.model}\n    if input_type is not None:\n        payload[\"input_type\"] = input_type\n\n    response = await self._client.post(\n        f\"{self._base_url}/embeddings\",\n        headers={\n            \"Authorization\": f\"Bearer {self._api_key}\",\n            \"Content-Type\": \"application/json\",\n        },\n        json=payload,\n    )\n    if response.status_code &gt;= httpx.codes.BAD_REQUEST:\n        msg = f\"Voyage API error {response.status_code}: {response.text}\"\n        raise VoyageError(msg)\n\n    data = response.json().get(\"data\", [])\n    return [item[\"embedding\"] for item in data]\n</code></pre>"},{"location":"api/ragsmith/embeddings/#ragsmith.embeddings.VoyageError","title":"VoyageError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised when the Voyage API returns an error payload.</p>"},{"location":"api/ragsmith/retriever/","title":"<code>ragsmith.retriever</code>","text":""},{"location":"api/ragsmith/retriever/#ragsmith.retriever","title":"ragsmith.retriever","text":"<p>High-level retriever wiring an embeddings client and a vector store.</p>"},{"location":"api/ragsmith/retriever/#ragsmith.retriever.RetrievedChunk","title":"RetrievedChunk  <code>dataclass</code>","text":"<pre><code>RetrievedChunk(document: Document, score: float)\n</code></pre> <p>A single retrieval result.</p> <p>Attributes:</p> Name Type Description <code>document</code> <code>Document</code> <p>The matched document.</p> <code>score</code> <code>float</code> <p>Cosine similarity score in <code>[0, 1]</code> (<code>1 - distance</code>).</p>"},{"location":"api/ragsmith/retriever/#ragsmith.retriever.Retriever","title":"Retriever","text":"<pre><code>Retriever(embeddings: VoyageClient, store: PgVectorStore)\n</code></pre> <p>Embed a query and look it up in a <code>PgVectorStore</code>.</p> <p>Build a retriever.</p> <p>Parameters:</p> Name Type Description Default <code>embeddings</code> <code>VoyageClient</code> <p>The Voyage client used for query embeddings.</p> required <code>store</code> <code>PgVectorStore</code> <p>The destination vector store.</p> required Source code in <code>src/ragsmith/retriever.py</code> <pre><code>def __init__(self, embeddings: VoyageClient, store: PgVectorStore) -&gt; None:\n    \"\"\"Build a retriever.\n\n    Args:\n        embeddings: The Voyage client used for query embeddings.\n        store: The destination vector store.\n    \"\"\"\n    self._embeddings = embeddings\n    self._store = store\n</code></pre>"},{"location":"api/ragsmith/retriever/#ragsmith.retriever.Retriever.retrieve","title":"retrieve  <code>async</code>","text":"<pre><code>retrieve(query: str, *, k: int = 5) -&gt; list[RetrievedChunk]\n</code></pre> <p>Embed <code>query</code> and return the top-<code>k</code> similar documents.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>User query.</p> required <code>k</code> <code>int</code> <p>Maximum number of documents to return.</p> <code>5</code> <p>Returns:</p> Type Description <code>list[RetrievedChunk]</code> <p>Ordered list of <code>RetrievedChunk</code>.</p> Source code in <code>src/ragsmith/retriever.py</code> <pre><code>async def retrieve(self, query: str, *, k: int = 5) -&gt; list[RetrievedChunk]:\n    \"\"\"Embed ``query`` and return the top-``k`` similar documents.\n\n    Args:\n        query: User query.\n        k: Maximum number of documents to return.\n\n    Returns:\n        Ordered list of [`RetrievedChunk`][ragsmith.retriever.RetrievedChunk].\n    \"\"\"\n    vectors = await self._embeddings.embed([query], input_type=\"query\")\n    hits = await self._store.similarity_search(vectors[0], k=k)\n    return [RetrievedChunk(document=doc, score=1.0 - distance) for doc, distance in hits]\n</code></pre>"},{"location":"api/ragsmith/store/","title":"<code>ragsmith.store</code>","text":""},{"location":"api/ragsmith/store/#ragsmith.store","title":"ragsmith.store","text":"<p>Postgres + pgvector storage backed by <code>asyncpg</code>.</p> <p>We avoid the optional <code>pgvector</code> Python package (which can pull <code>numpy</code>) and instead serialize vectors as <code>'[v1,v2,...]'::vector</code> literals.</p>"},{"location":"api/ragsmith/store/#ragsmith.store.Document","title":"Document  <code>dataclass</code>","text":"<pre><code>Document(\n    content: str,\n    embedding: list[float],\n    metadata: dict[str, object] = dict(),\n    doc_id: UUID = uuid4(),\n)\n</code></pre> <p>A chunk persisted in the vector store.</p> <p>Attributes:</p> Name Type Description <code>content</code> <code>str</code> <p>Raw text content.</p> <code>embedding</code> <code>list[float]</code> <p>Embedding vector (length must match the store <code>dim</code>).</p> <code>metadata</code> <code>dict[str, object]</code> <p>Arbitrary JSON-serializable metadata.</p> <code>doc_id</code> <code>UUID</code> <p>Stable identifier (auto-generated when omitted).</p>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore","title":"PgVectorStore","text":"<pre><code>PgVectorStore(pool: Pool, *, table: str, dim: int)\n</code></pre> <p>Async pgvector-backed document store.</p> <p>The store creates (idempotently) a single table with an <code>ivfflat</code> index on cosine distance. Use <code>from_dsn</code> to obtain a fully initialized instance.</p> <p>Build a new store bound to an existing pool.</p> <p>Parameters:</p> Name Type Description Default <code>pool</code> <code>Pool</code> <p>An <code>asyncpg</code> connection pool.</p> required <code>table</code> <code>str</code> <p>Destination table name (validated to be a SQL identifier).</p> required <code>dim</code> <code>int</code> <p>Embedding dimensionality.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>table</code> is not a safe identifier or <code>dim</code> &lt;= 0.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>def __init__(self, pool: asyncpg.Pool, *, table: str, dim: int) -&gt; None:\n    \"\"\"Build a new store bound to an existing pool.\n\n    Args:\n        pool: An ``asyncpg`` connection pool.\n        table: Destination table name (validated to be a SQL identifier).\n        dim: Embedding dimensionality.\n\n    Raises:\n        ValueError: If ``table`` is not a safe identifier or ``dim`` &lt;= 0.\n    \"\"\"\n    if not table.replace(\"_\", \"\").isalnum():\n        msg = f\"unsafe table name: {table!r}\"\n        raise ValueError(msg)\n    if dim &lt;= 0:\n        msg = \"dim must be &gt; 0\"\n        raise ValueError(msg)\n    self._pool = pool\n    self._table = table\n    self._dim = dim\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__() -&gt; Self\n</code></pre> <p>Enter async context manager.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def __aenter__(self) -&gt; Self:\n    \"\"\"Enter async context manager.\"\"\"\n    return self\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None\n</code></pre> <p>Close the underlying pool.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def __aexit__(\n    self,\n    exc_type: type[BaseException] | None,\n    exc: BaseException | None,\n    tb: TracebackType | None,\n) -&gt; None:\n    \"\"\"Close the underlying pool.\"\"\"\n    await self.aclose()\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying connection pool.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def aclose(self) -&gt; None:\n    \"\"\"Close the underlying connection pool.\"\"\"\n    await self._pool.close()\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.from_dsn","title":"from_dsn  <code>async</code> <code>classmethod</code>","text":"<pre><code>from_dsn(\n    dsn: str,\n    *,\n    table: str = \"ragsmith_chunks\",\n    dim: int = 1024,\n) -&gt; Self\n</code></pre> <p>Create a store and the underlying pool from a DSN.</p> <p>Parameters:</p> Name Type Description Default <code>dsn</code> <code>str</code> <p>Postgres connection string.</p> required <code>table</code> <code>str</code> <p>Destination table name.</p> <code>'ragsmith_chunks'</code> <code>dim</code> <code>int</code> <p>Embedding dimensionality.</p> <code>1024</code> <p>Returns:</p> Type Description <code>Self</code> <p>An initialized <code>PgVectorStore</code>.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>@classmethod\nasync def from_dsn(\n    cls,\n    dsn: str,\n    *,\n    table: str = \"ragsmith_chunks\",\n    dim: int = 1024,\n) -&gt; Self:\n    \"\"\"Create a store and the underlying pool from a DSN.\n\n    Args:\n        dsn: Postgres connection string.\n        table: Destination table name.\n        dim: Embedding dimensionality.\n\n    Returns:\n        An initialized [`PgVectorStore`][ragsmith.store.PgVectorStore].\n    \"\"\"\n    import asyncpg  # noqa: PLC0415\n\n    pool = await asyncpg.create_pool(dsn=dsn)\n    store = cls(pool, table=table, dim=dim)\n    await store.initialize()\n    return store\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.initialize","title":"initialize  <code>async</code>","text":"<pre><code>initialize() -&gt; None\n</code></pre> <p>Create the pgvector extension, table and index if missing.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def initialize(self) -&gt; None:\n    \"\"\"Create the pgvector extension, table and index if missing.\"\"\"\n    async with self._pool.acquire() as conn:\n        await conn.execute(\"CREATE EXTENSION IF NOT EXISTS vector\")\n        await conn.execute(\n            f\"CREATE TABLE IF NOT EXISTS {self._table} (\"\n            \"  id UUID PRIMARY KEY,\"\n            \"  content TEXT NOT NULL,\"\n            \"  metadata JSONB NOT NULL DEFAULT '{}'::jsonb,\"\n            f\"  embedding vector({self._dim}) NOT NULL\"\n            \")\",\n        )\n        await conn.execute(\n            f\"CREATE INDEX IF NOT EXISTS {self._table}_embedding_idx \"\n            f\"ON {self._table} USING ivfflat (embedding vector_cosine_ops)\",\n        )\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.similarity_search","title":"similarity_search  <code>async</code>","text":"<pre><code>similarity_search(\n    embedding: Sequence[float], *, k: int = 5\n) -&gt; list[tuple[Document, float]]\n</code></pre> <p>Return the <code>k</code> documents closest to <code>embedding</code> (cosine distance).</p> <p>Parameters:</p> Name Type Description Default <code>embedding</code> <code>Sequence[float]</code> <p>Query vector.</p> required <code>k</code> <code>int</code> <p>Number of results.</p> <code>5</code> <p>Returns:</p> Type Description <code>list[tuple[Document, float]]</code> <p>Pairs of (document, distance) sorted by ascending distance.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def similarity_search(\n    self,\n    embedding: Sequence[float],\n    *,\n    k: int = 5,\n) -&gt; list[tuple[Document, float]]:\n    \"\"\"Return the ``k`` documents closest to ``embedding`` (cosine distance).\n\n    Args:\n        embedding: Query vector.\n        k: Number of results.\n\n    Returns:\n        Pairs of (document, distance) sorted by ascending distance.\n    \"\"\"\n    async with self._pool.acquire() as conn:\n        records = await conn.fetch(\n            f\"SELECT id, content, metadata, embedding, \"  # noqa: S608\n            f\"embedding &lt;=&gt; $1::vector AS distance \"\n            f\"FROM {self._table} ORDER BY distance ASC LIMIT $2\",\n            _vector_literal(embedding),\n            k,\n        )\n    results: list[tuple[Document, float]] = []\n    for record in records:\n        doc = Document(\n            doc_id=UUID(str(record[\"id\"])),\n            content=record[\"content\"],\n            metadata=json.loads(record[\"metadata\"]) if record[\"metadata\"] else {},\n            embedding=[],\n        )\n        results.append((doc, float(record[\"distance\"])))\n    return results\n</code></pre>"},{"location":"api/ragsmith/store/#ragsmith.store.PgVectorStore.upsert","title":"upsert  <code>async</code>","text":"<pre><code>upsert(documents: Iterable[Document]) -&gt; int\n</code></pre> <p>Insert or update documents.</p> <p>Parameters:</p> Name Type Description Default <code>documents</code> <code>Iterable[Document]</code> <p>Documents to persist.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The number of rows written.</p> Source code in <code>src/ragsmith/store.py</code> <pre><code>async def upsert(self, documents: Iterable[Document]) -&gt; int:\n    \"\"\"Insert or update documents.\n\n    Args:\n        documents: Documents to persist.\n\n    Returns:\n        The number of rows written.\n    \"\"\"\n    rows = [\n        (str(d.doc_id), d.content, json.dumps(d.metadata), _vector_literal(d.embedding))\n        for d in documents\n    ]\n    if not rows:\n        return 0\n    async with self._pool.acquire() as conn:\n        await conn.executemany(\n            f\"INSERT INTO {self._table} (id, content, metadata, embedding) \"  # noqa: S608\n            \"VALUES ($1, $2, $3::jsonb, $4::vector) \"\n            \"ON CONFLICT (id) DO UPDATE SET \"\n            \"content = EXCLUDED.content, \"\n            \"metadata = EXCLUDED.metadata, \"\n            \"embedding = EXCLUDED.embedding\",\n            rows,\n        )\n    return len(rows)\n</code></pre>"}]}